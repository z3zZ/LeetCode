#in place is the name for multiple types of sorts that share the same memory as one another,
#meaning I can use an insertion sort

#make the array only hold the values of "val" this could technically also mean to just read all occurances of val or to delte everything but val in the array and then count the amount of occurances (K) that are within the array

#print/return K
#Do not worry about size


class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        

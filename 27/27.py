#remove the given value from the the array using the "in place algorithm" to make a new array
#Then return the amount of inputs within our new nums that are remaining without val
#Make the variable of whats remaining be called K and then print K
#Do not worry about size


class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:

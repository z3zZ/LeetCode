#input target # x (int)
#find for palindrome (if it reads the same forwards and backwards)
#use a two-pointer system to test the values are the same both left and right position
#if it is print yes,
## took roughly 7mins to figure out

class Solution:
    def isPalindrome(self, x: int) -> bool:
        